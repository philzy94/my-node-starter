/**
 * MyProductService
 * Service for handling business logic related to myProduct.
 * Add your methods and dependencies here.
 */
import { injectable } from 'tsyringe';

/**
 * Injectable service for myProduct operations.
 */
@injectable()
export class MyProductService {
  /**
   * Add dependencies to the constructor if needed.
   */
  constructor() {}
}
